{
  "name": "lance-namespace-typescript-workspace",
  "private": true,
  "version": "0.5.2",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  }
}
