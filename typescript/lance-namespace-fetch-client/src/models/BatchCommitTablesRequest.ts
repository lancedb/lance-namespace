/* tslint:disable */
/* eslint-disable */
/**
 * Lance Namespace Specification
 * This OpenAPI specification is a part of the Lance namespace specification. It contains 2 parts:  The `components/schemas`, `components/responses`, `components/examples`, `tags` sections define the request and response shape for each operation in a Lance Namespace across all implementations. See https://lance.org/format/namespace/operations for more details.  The `servers`, `security`, `paths`, `components/parameters` sections are for the Lance REST Namespace implementation, which defines a complete REST server that can work with Lance datasets. See https://lance.org/format/namespace/rest for more details. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { CommitTableOperation } from './CommitTableOperation';
import {
    CommitTableOperationFromJSON,
    CommitTableOperationFromJSONTyped,
    CommitTableOperationToJSON,
    CommitTableOperationToJSONTyped,
} from './CommitTableOperation';
import type { Identity } from './Identity';
import {
    IdentityFromJSON,
    IdentityFromJSONTyped,
    IdentityToJSON,
    IdentityToJSONTyped,
} from './Identity';

/**
 * Request to atomically commit a batch of table operations.
 * This replaces `BatchCreateTableVersionsRequest` with a more general interface
 * that supports mixed operations (DeclareTable, CreateTableVersion,
 * DeleteTableVersions, DeregisterTable)
 * within a single atomic transaction at the metadata layer.
 * 
 * All operations are committed atomically: either all succeed or none are applied.
 * 
 * @export
 * @interface BatchCommitTablesRequest
 */
export interface BatchCommitTablesRequest {
    /**
     * 
     * @type {Identity}
     * @memberof BatchCommitTablesRequest
     */
    identity?: Identity;
    /**
     * Arbitrary context as key-value pairs.
     * How to use the context is custom to the specific implementation.
     * 
     * On a request, it carries caller-provided context to the implementation.
     * On a response, it carries implementation-provided context back to the caller.
     * 
     * REST NAMESPACE ONLY
     * Context entries are mapped to and from HTTP headers using the `header.` prefix:
     * - On a request, any entry whose key starts with `header.` is sent as an HTTP
     *   request header with the prefix stripped. For example, the entry
     *   `{"header.Authorization": "Bearer abc"}` is sent as the request header
     *   `Authorization: Bearer abc`.
     * - On a response, every HTTP response header is returned as an entry whose key is the
     *   header name prefixed with `header.`. For example, the response header
     *   `x-request-id: abc123` is returned as the entry `{"header.x-request-id": "abc123"}`.
     * 
     * @type {{ [key: string]: string; }}
     * @memberof BatchCommitTablesRequest
     */
    context?: { [key: string]: string; };
    /**
     * List of operations to commit atomically.
     * Supported operation types: DeclareTable, CreateTableVersion,
     * DeleteTableVersions, DeregisterTable.
     * 
     * @type {Array<CommitTableOperation>}
     * @memberof BatchCommitTablesRequest
     */
    operations: Array<CommitTableOperation>;
}

/**
 * Check if a given object implements the BatchCommitTablesRequest interface.
 */
export function instanceOfBatchCommitTablesRequest(value: object): value is BatchCommitTablesRequest {
    if (!('operations' in value) || value['operations'] === undefined) return false;
    return true;
}

export function BatchCommitTablesRequestFromJSON(json: any): BatchCommitTablesRequest {
    return BatchCommitTablesRequestFromJSONTyped(json, false);
}

export function BatchCommitTablesRequestFromJSONTyped(json: any, ignoreDiscriminator: boolean): BatchCommitTablesRequest {
    if (json == null) {
        return json;
    }
    return {
        
        'identity': json['identity'] == null ? undefined : IdentityFromJSON(json['identity']),
        'context': json['context'] == null ? undefined : json['context'],
        'operations': ((json['operations'] as Array<any>).map(CommitTableOperationFromJSON)),
    };
}

export function BatchCommitTablesRequestToJSON(json: any): BatchCommitTablesRequest {
    return BatchCommitTablesRequestToJSONTyped(json, false);
}

export function BatchCommitTablesRequestToJSONTyped(value?: BatchCommitTablesRequest | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'identity': IdentityToJSON(value['identity']),
        'context': value['context'],
        'operations': ((value['operations'] as Array<any>).map(CommitTableOperationToJSON)),
    };
}

