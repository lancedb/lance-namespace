/* tslint:disable */
/* eslint-disable */
/**
 * Lance Namespace Specification
 * This OpenAPI specification is a part of the Lance namespace specification. It contains 2 parts:  The `components/schemas`, `components/responses`, `components/examples`, `tags` sections define the request and response shape for each operation in a Lance Namespace across all implementations. See https://lance.org/format/namespace/operations for more details.  The `servers`, `security`, `paths`, `components/parameters` sections are for the Lance REST Namespace implementation, which defines a complete REST server that can work with Lance datasets. See https://lance.org/format/namespace/rest for more details. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { JsonArrowSchema } from './JsonArrowSchema';
import {
    JsonArrowSchemaFromJSON,
    JsonArrowSchemaFromJSONTyped,
    JsonArrowSchemaToJSON,
    JsonArrowSchemaToJSONTyped,
} from './JsonArrowSchema';
import type { TableBasicStats } from './TableBasicStats';
import {
    TableBasicStatsFromJSON,
    TableBasicStatsFromJSONTyped,
    TableBasicStatsToJSON,
    TableBasicStatsToJSONTyped,
} from './TableBasicStats';

/**
 * 
 * @export
 * @interface DescribeTableResponse
 */
export interface DescribeTableResponse {
    /**
     * Arbitrary context as key-value pairs.
     * How to use the context is custom to the specific implementation.
     * 
     * On a request, it carries caller-provided context to the implementation.
     * On a response, it carries implementation-provided context back to the caller.
     * 
     * REST NAMESPACE ONLY
     * Context entries are mapped to and from HTTP headers using the `header.` prefix:
     * - On a request, any entry whose key starts with `header.` is sent as an HTTP
     *   request header with the prefix stripped. For example, the entry
     *   `{"header.Authorization": "Bearer abc"}` is sent as the request header
     *   `Authorization: Bearer abc`.
     * - On a response, every HTTP response header is returned as an entry whose key is the
     *   header name prefixed with `header.`. For example, the response header
     *   `x-request-id: abc123` is returned as the entry `{"header.x-request-id": "abc123"}`.
     * 
     * @type {{ [key: string]: string; }}
     * @memberof DescribeTableResponse
     */
    context?: { [key: string]: string; };
    /**
     * Table name.
     * Only populated when `load_detailed_metadata` is true.
     * 
     * @type {string}
     * @memberof DescribeTableResponse
     */
    table?: string;
    /**
     * The namespace identifier as a list of parts.
     * Only populated when `load_detailed_metadata` is true.
     * 
     * @type {Array<string>}
     * @memberof DescribeTableResponse
     */
    namespace?: Array<string>;
    /**
     * Table version number.
     * Only populated when `load_detailed_metadata` is true.
     * 
     * @type {number}
     * @memberof DescribeTableResponse
     */
    version?: number;
    /**
     * Table storage location (e.g., S3/GCS path).
     * 
     * @type {string}
     * @memberof DescribeTableResponse
     */
    location?: string;
    /**
     * Table URI. Unlike location, this field must be a complete and valid URI.
     * Only returned when `with_table_uri` is true.
     * 
     * @type {string}
     * @memberof DescribeTableResponse
     */
    table_uri?: string;
    /**
     * Table schema in JSON Arrow format.
     * Only populated when `load_detailed_metadata` is true.
     * 
     * @type {JsonArrowSchema}
     * @memberof DescribeTableResponse
     */
    schema?: JsonArrowSchema;
    /**
     * Configuration options to be used to access storage. The available
     * options depend on the type of storage in use. These will be
     * passed directly to Lance to initialize storage access.
     * When `vend_credentials` is true, this field may include vended credentials.
     * If the vended credentials are temporary, the `expires_at_millis` key should be
     * included to indicate the millisecond timestamp when the credentials expire.
     * 
     * @type {{ [key: string]: string; }}
     * @memberof DescribeTableResponse
     */
    storage_options?: { [key: string]: string; };
    /**
     * Table statistics.
     * Only populated when `load_detailed_metadata` is true.
     * 
     * @type {TableBasicStats}
     * @memberof DescribeTableResponse
     */
    stats?: TableBasicStats;
    /**
     * Optional table metadata as key-value pairs. This records the information of the table
     * and requires loading the table.
     * It is only populated when `load_detailed_metadata` is true.
     * 
     * @type {{ [key: string]: string; }}
     * @memberof DescribeTableResponse
     */
    metadata?: { [key: string]: string; };
    /**
     * Properties stored on the table, if supported by the server. This records the information managed by the namespace. If the server does not support table properties, it should return null for this field. If table properties are supported, but none are set, it should return an empty object.
     * @type {{ [key: string]: string; }}
     * @memberof DescribeTableResponse
     */
    properties?: { [key: string]: string; };
    /**
     * When true, the caller should use namespace table version operations
     * (CreateTableVersion, BatchCreateTableVersions, DescribeTableVersion, ListTableVersions, BatchDeleteTableVersions)
     * to manage table versions instead of relying on Lance's native version management.
     * 
     * @type {boolean}
     * @memberof DescribeTableResponse
     */
    managed_versioning?: boolean;
    /**
     * When true, indicates that the table has been declared in the namespace
     * but not yet created on storage. This means the table exists in the
     * namespace but has no data files on the underlying storage.
     * When false, the table has storage components (data and metadata files).
     * When null, the implementation did not check whether the table is only
     * declared. Clients should treat an omitted value as null. Implementations
     * should populate this field when `check_declared` is true or another
     * option such as `load_detailed_metadata` requires checking declared-only
     * table state. Operations like describe_table with load_detailed_metadata=true
     * may fail for declared-only tables.
     * 
     * @type {boolean}
     * @memberof DescribeTableResponse
     */
    is_only_declared?: boolean;
}

/**
 * Check if a given object implements the DescribeTableResponse interface.
 */
export function instanceOfDescribeTableResponse(value: object): value is DescribeTableResponse {
    return true;
}

export function DescribeTableResponseFromJSON(json: any): DescribeTableResponse {
    return DescribeTableResponseFromJSONTyped(json, false);
}

export function DescribeTableResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): DescribeTableResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'context': json['context'] == null ? undefined : json['context'],
        'table': json['table'] == null ? undefined : json['table'],
        'namespace': json['namespace'] == null ? undefined : json['namespace'],
        'version': json['version'] == null ? undefined : json['version'],
        'location': json['location'] == null ? undefined : json['location'],
        'table_uri': json['table_uri'] == null ? undefined : json['table_uri'],
        'schema': json['schema'] == null ? undefined : JsonArrowSchemaFromJSON(json['schema']),
        'storage_options': json['storage_options'] == null ? undefined : json['storage_options'],
        'stats': json['stats'] == null ? undefined : TableBasicStatsFromJSON(json['stats']),
        'metadata': json['metadata'] == null ? undefined : json['metadata'],
        'properties': json['properties'] == null ? undefined : json['properties'],
        'managed_versioning': json['managed_versioning'] == null ? undefined : json['managed_versioning'],
        'is_only_declared': json['is_only_declared'] == null ? undefined : json['is_only_declared'],
    };
}

export function DescribeTableResponseToJSON(json: any): DescribeTableResponse {
    return DescribeTableResponseToJSONTyped(json, false);
}

export function DescribeTableResponseToJSONTyped(value?: DescribeTableResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'context': value['context'],
        'table': value['table'],
        'namespace': value['namespace'],
        'version': value['version'],
        'location': value['location'],
        'table_uri': value['table_uri'],
        'schema': JsonArrowSchemaToJSON(value['schema']),
        'storage_options': value['storage_options'],
        'stats': TableBasicStatsToJSON(value['stats']),
        'metadata': value['metadata'],
        'properties': value['properties'],
        'managed_versioning': value['managed_versioning'],
        'is_only_declared': value['is_only_declared'],
    };
}

