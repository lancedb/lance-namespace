# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

.PHONY: lint
lint:
	uv run openapi-spec-validator --errors all docs/src/spec.yaml

.PHONY: clean-rust
clean-rust:
	cd rust; make clean

.PHONY: sync gen-rust
gen-rust:
	cd rust; make gen

.PHONY: build-rust
build-rust:
	cd rust; make build

.PHONY: clean-python
clean-python:
	cd python; make clean

.PHONY: sync gen-python
gen-python:
	cd python; make gen

.PHONY: build-python
build-python:
	cd python; make build

.PHONY: clean-java
clean-java:
	cd java; make clean

.PHONY: gen-java
gen-java:
	cd java; make gen

.PHONY: build-java
build-java:
	cd java; make build

.PHONY: build-docs
build-docs: gen-java
	cd docs; make build

.PHONY: serve-docs
serve-docs: gen-java
	cd docs; make serve

.PHONY: sync
sync:
	uv sync --all-packages

.PHONY: clean
clean: clean-rust clean-python clean-java

.PHONY: gen
gen: lint gen-rust gen-python gen-java

.PHONY: build
build: lint build-docs build-rust build-python build-java build-cts-wiremock

# ============================================================
# CTS (Contract Test Suite) targets
# ============================================================

# Variables
SPEC_SRC          := docs/src/spec.yaml
AUTO_OVERLAY      := build/overlays/examples.auto.yaml
SPEC_MERGED       := build/spec.merged.yaml
CTS_OUT           := build/cts
WIREMOCK_VER      := 3.9.1
WIREMOCK_JAR      := $(CTS_OUT)/wiremock-standalone.jar
WIREMOCK_MAPPINGS := $(CTS_OUT)/wiremock/src/main/resources/mappings

# Generate examples overlay from spec (read-only on spec)
$(AUTO_OVERLAY): $(SPEC_SRC)
	@mkdir -p build/overlays
	uv run python ci/cts/gen_examples_overlay.py \
		--spec $(SPEC_SRC) \
		--output $(AUTO_OVERLAY)

# Merge spec with overlay to produce annotated spec
$(SPEC_MERGED): $(AUTO_OVERLAY) $(SPEC_SRC)
	@mkdir -p build
	uv run python ci/cts/apply_overlay.py \
		$(SPEC_SRC) $(AUTO_OVERLAY) > $(SPEC_MERGED)

merge-spec: $(SPEC_MERGED)
.PHONY: merge-spec

# Generate WireMock stubs from spec for all operations.
# gen_wiremock_mappings.py reads spec.yaml and produces one JSON mapping per
# operation (49 total).  Requires only pyyaml (already a workspace dependency).
gen-wiremock: $(SPEC_MERGED)
	@mkdir -p $(WIREMOCK_MAPPINGS)
	uv run python ci/cts/gen_wiremock_mappings.py \
		--spec $(SPEC_SRC) \
		--output $(WIREMOCK_MAPPINGS)
	@ls $(WIREMOCK_MAPPINGS)/*.json >/dev/null 2>&1 || \
		(echo "ERROR: gen_wiremock_mappings.py produced no mapping files" && exit 1)

.PHONY: gen-wiremock

# Download WireMock standalone jar
$(WIREMOCK_JAR):
	@mkdir -p $(CTS_OUT)
	curl -fsSL \
	  https://repo1.maven.org/maven2/org/wiremock/wiremock-standalone/$(WIREMOCK_VER)/wiremock-standalone-$(WIREMOCK_VER).jar \
	  -o $(WIREMOCK_JAR)

# Generate WireMock contract test files for all 4 clients (Rust, Python, Java Apache, Java Async).
# Depends on gen-wiremock so the mappings directory already exists when the script runs.
gen-wiremock-tests: gen-wiremock
	uv run python ci/cts/gen_wiremock_tests.py \
		--mappings-dir $(WIREMOCK_MAPPINGS) \
		--out-rust rust/lance-namespace-reqwest-client/tests/wiremock.rs \
		--out-python python/lance_namespace_urllib3_client/tests/test_wiremock.py \
		--out-java-apache java/lance-namespace-apache-client/src/test/java/org/lance/namespace/client/apache/cts/WireMockIT.java \
		--out-java-async java/lance-namespace-async-client/src/test/java/org/lance/namespace/client/async/cts/WireMockIT.java
	# Apply spotless formatting to the generated Java test files so the committed
	# version satisfies spotless:check on CI (which does not run apply beforehand).
	cd java && ./mvnw -q spotless:apply \
		-pl lance-namespace-apache-client,lance-namespace-async-client -am \
		-DskipTests || true

.PHONY: gen-wiremock-tests

# Run all WireMock-CTS generation steps (spec overlay merge, WireMock JSON
# mappings, per-client wire-level contract test sources, and the WireMock
# standalone jar).  Strictly scoped to the WireMock layer — the behavioural
# CTS suite is regenerated by `gen-cts-behavior` instead.
gen-cts-wiremock: $(AUTO_OVERLAY) $(SPEC_MERGED) gen-wiremock gen-wiremock-tests $(WIREMOCK_JAR)
	@echo "CTS WireMock artifacts generated in $(CTS_OUT)"

.PHONY: gen-cts-wiremock

# Regenerate WireMock CTS artifacts (spec merge, WireMock stubs, per-client
# wire-level contract tests) AFTER the clients have been (re)generated and
# built.
#
# Order matters: each per-language `build-*` target depends on a `gen-*-client`
# target that internally does `rm -rf <client-dir>/**` followed by
# openapi-generator.  If we ran `gen-cts-wiremock` first, the generated
# contract test files (which live inside those very client directories)
# would be wiped out moments later.  Therefore: build clients first, then
# drop the contract test files into the freshly produced trees.
#
# Finally, run `test-compile` on the two Java client modules so the freshly
# written `WireMockIT.java` is actually compiled (the per-module `build`
# targets above only ran `mvn install`, which happens *before* the contract
# tests are written).
build-cts-wiremock: build-rust build-python build-java gen-cts-wiremock
	cd java && ./mvnw -q -pl lance-namespace-apache-client,lance-namespace-async-client \
		test-compile -DskipTests --no-transfer-progress
	@echo "CTS WireMock build complete"

.PHONY: build-cts-wiremock

# Spec lint
test-spec-lint: verify-spec-untouched
	@mkdir -p build
	npx --yes @stoplight/spectral-cli lint $(SPEC_SRC) \
		--ruleset ci/spectral.yaml \
		--fail-severity error \
		--format junit \
		--output build/spectral-report.xml
	uv run python ci/cts/lint_contracts.py

.PHONY: test-spec-lint

# Schemathesis server conformance test
# Requires: BASE_URL env var pointing to a running server, and ci/cts/schemathesis.toml
test-schemathesis: $(SPEC_MERGED)
	@mkdir -p build/reports
	uv run schemathesis --config-file ci/cts/schemathesis.toml run $(SPEC_MERGED) \
		--url $${BASE_URL:-http://localhost:8080} \
		--report-junit-path build/reports/schemathesis.xml

.PHONY: test-schemathesis

# Java client WireMock contract tests
test-clients-wiremock-java: gen-wiremock $(WIREMOCK_JAR)
	cd java && ./mvnw -pl lance-namespace-apache-client,lance-namespace-async-client \
		test -Dtest="WireMockIT" \
		-Dsurefire.failIfNoSpecifiedTests=false \
		--no-transfer-progress

.PHONY: test-clients-wiremock-java

# Python client WireMock contract tests
test-clients-wiremock-python: gen-wiremock $(WIREMOCK_JAR)
	cd python && uv run pytest \
		lance_namespace_urllib3_client/tests/test_wiremock.py \
		-v --tb=short

.PHONY: test-clients-wiremock-python

# Rust client WireMock contract tests
test-clients-wiremock-rust: gen-wiremock $(WIREMOCK_JAR)
	cd rust/lance-namespace-reqwest-client && \
		cargo test --test wiremock -- --nocapture

.PHONY: test-clients-wiremock-rust

# All client WireMock tests
test-clients-wiremock: test-clients-wiremock-java test-clients-wiremock-python test-clients-wiremock-rust
.PHONY: test-clients-wiremock

# `make test-cts` runs the full local CTS matrix: spec lint, the
# behavioural-contract suite (in-process Rust harness against
# `DirectoryNamespace`), and the WireMock wire-shape suite across
# all four clients.  This mirrors what CI executes end-to-end so a
# clean local `make test-cts` is a strong pre-push signal.
#
# The two layers remain independently invokable:
#   * `make test-cts-behavior`  — fast, no JVM, PR-blocking in CI.
#   * `make test-cts-wiremock`  — wire-shape only, requires JDK/Maven
#                                 and rebuilds all clients.
# CI keeps PR-blocking limited to the behavioural job; the WireMock
# job runs in parallel as a non-blocking signal (see
# .github/workflows/contract-tests.yml).
test-cts: test-spec-lint test-cts-behavior test-cts-wiremock
.PHONY: test-cts

# Wire-shape-only CTS: regenerates clients, drops the per-client
# WireMock contract tests into their trees, and runs them against a
# stub server.  Heavier than `test-cts-behavior` (needs JDK + Maven
# and rebuilds Rust/Python/Java clients), so it is also exposed as a
# standalone target for contributors who only want the wire layer.
test-cts-wiremock: test-spec-lint build-cts-wiremock test-clients-wiremock
.PHONY: test-cts-wiremock

# ============================================================
# Behavioural-contract CTS
#
# These targets drive the in-process Rust harness in
# `rust/lance-namespace-cts`, which depends via cargo path on the
# sibling `lance` repository's `lance-namespace-impls` crate.  No
# WireMock, no JVM, no Spring Boot — just `cargo test`.
# ============================================================

# Regenerate the per-operation contract test sources from
# `docs/src/cts-contracts/*.yaml`.  Idempotent; safe to re-run.
gen-cts-behavior:
	uv run python ci/cts/gen_contract_tests.py

.PHONY: gen-cts-behavior

# Run the in-process behavioural contract suite.  Depends on
# gen-cts-behavior so a fresh checkout works without manual steps.
test-cts-behavior: gen-cts-behavior
	cd rust && cargo test -p lance-namespace-cts --test cts -- --nocapture

.PHONY: test-cts-behavior

# Verify spec was not modified
verify-spec-untouched:
	@git diff --exit-code -- $(SPEC_SRC) || \
		(echo "ERROR: $(SPEC_SRC) was modified. This file must not be changed." && exit 1)
	@echo "OK: $(SPEC_SRC) is untouched"

.PHONY: verify-spec-untouched

